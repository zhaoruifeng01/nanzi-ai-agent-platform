[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
markers =
    no_infrastructure: skip shared database and redis initialization for isolated unit tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
